package com.next.engine.data;

/**
 * A common interface for data structures that serve as <i>buffers</i> and want to be managed by a {@link BufferStrategy}.
 */
public interface Buffered {
    void clear();
}
