package com.next.model;

public class Entity extends Actor {

    public Entity(int spriteId) {
        super();
        this.spriteId = spriteId;
    }

    public void update(double delta) {
    }
}
